# URL of your RAIS-compatible backend (Next.js, Express, FastAPI, etc.)
# With the Vite proxy above, /api → http://localhost:3001, so just set:
VITE_API_URL=/api/chat
# Or point directly to a remote RAIS endpoint:
# VITE_API_URL=https://your-backend.vercel.app/api/chat
